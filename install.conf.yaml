# Install with https://github.com/anishathalye/dotbot

- clean: ["~"]
- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.config/: 
      path: dotconfs/config/*
      glob: true
    ~/:
      path: dotconfs/*
      prefix: '.'
      glob: true
      exclude: ['dotconfs/config']
